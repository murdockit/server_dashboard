services:
  dashboard:
    build: .
    ports:
      - "80:8000"
    environment:
      - PORT=8000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
    pid: host
